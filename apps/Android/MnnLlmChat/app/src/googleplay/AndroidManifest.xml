<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Remove REQUEST_INSTALL_PACKAGES permission for Google Play build -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" tools:node="remove" />
    
    <!-- Remove FOREGROUND_SERVICE permissions for Google Play build -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" tools:node="remove" />


    <application>
        <service
            android:name="com.alibaba.mls.api.download.DownloadForegroundService"
            tools:node="remove" />
        
        <service
            android:name="com.alibaba.mnnllm.api.openai.service.OpenAIService"
            android:enabled="true"
            android:exported="true"
            tools:node="replace" />
    </application> -->

</manifest> 
package com.alibaba.mnnllm.android.api

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Ignore
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Phase-2 placeholder for UiAutomator-based thinking-mode switch regression.
 * This test is intentionally disabled until the full UI flow is implemented.
 */
@RunWith(AndroidJUnit4::class)
@Ignore("Phase-2 skeleton only; implementation pending.")
class ThinkingModeUiAutomatorSkeletonTest {

    @Test
    fun placeholder_for_thinking_mode_switch_flow() {
        // Placeholder assertions for future implementation:
        // 1) Open chat page with Qwen3.5-0.8B.
        // 2) Toggle thinking button ON and verify model config state.
        // 3) Send prompt and assert response contains thinking tags.
        // 4) Toggle thinking button OFF and assert tags are absent.
    }
}
